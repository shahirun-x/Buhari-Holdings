# unrs-resolver ships prebuilt platform binaries, so its (ignored) postinstall
# build script is not required. Skip pnpm's implicit pre-run install check so
# `pnpm build` runs Next directly instead of failing on the ignored build.
verify-deps-before-run=false
